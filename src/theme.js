import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {		
		main: '#093C01',		    
    },
    secondary: {      
	  main: '#fdd835',	  
		},
		custom: {
		main: "#ffffff",
	}
  },
});
	  
export default theme;